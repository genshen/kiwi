# kiwi: A framework for parallel.
# author: genshen <genshenchu@gmail.com>
# pkg version 0.4.0

version: 2

args:

pkg: "git.hpcer.dev/genshen/kiwi"
target: "kiwi"

dependencies:
  packages:
    github.com/google/googletest: {version: release-1.8.0, target: GTest}
    github.com/fmtlib/fmt: {version: 5.3.0, target: fmt, features: ["FMT_TEST=OFF", "FMT_DOC=OFF"]}
